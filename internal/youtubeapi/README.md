# Cliente YouTube API (`internal/youtubeapi/`)

## O que é este diretório?
Wrapper tipado sobre o cliente oficial `google.golang.org/api/youtube/v3`.

## Para que serve?
Executa chamadas oficiais de busca, listagem de canais e paginação de playlists autenticadas via OAuth2.
