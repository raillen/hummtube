# Servidor Bridge Local (`internal/server/`)

## O que é este diretório?
Servidor HTTP e WebSocket local para comunicação IPC com a SPA.

## Para que serve?
Expõe os serviços Go para o frontend através de endpoints com verificação rígida de origem (`localhost` / `127.0.0.1`).
