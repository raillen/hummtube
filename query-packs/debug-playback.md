# Query Pack: Debug Playback

1. Verificar logs do console e devtools da WebView;
2. Testar resolução direta do `PlaybackResolver` com a URL do vídeo;
3. Inspecionar headers HTTP e formato retornado pelo `yt-dlp`;
4. Testar fallback para `InvidiousResolver`;
5. Verificar aceleração por hardware e suporte a codecs do WebView.
